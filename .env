# =====================================================================
# MCP Gateway + all MCP Servers — unified environment variables
# This is the SINGLE .env file for the entire workspace.
# =====================================================================

# --- Shared Huawei Cloud credentials ---
HUAWEICLOUD_ACCESS_KEY_ID=HPUA2LYSSORQNHTXQSWA
HUAWEICLOUD_SECRET_ACCESS_KEY=KCWHFCh4pRQYqFTydwPCOjY7Mmj9RlzoOh44f1pX
HUAWEICLOUD_PROJECT_ID=15f2d47addb14784b82eb910447250a9

# --- ECS MCP Server ---
HUAWEICLOUD_REGION=af-south-1

# --- CodeArts Pipeline MCP Server ---
CODEARTS_REGION=af-south-1
CODEARTS_DEFAULT_PROJECT_ID=ddb5e3259e81494f9d083c917e173e5b

# --- CTS MCP Server ---
# CTS reuses HUAWEICLOUD_REGION and HUAWEICLOUD_PROJECT_ID from above.
# CTS_DEFAULT_TIMEZONE=Asia/Shanghai

# --- MCP Gateway ---
MCP_GATEWAY_HOST=127.0.0.1
MCP_GATEWAY_PORT=8080
MCP_GATEWAY_LOG_LEVEL=info

# --- Gateway auth mode ---
# 当前为本地开发环境，使用 dev 模式（免 JWT，仅 loopback 放行）
# 生产环境改为: MCP_GATEWAY_AUTH_MODE=jwt
MCP_GATEWAY_AUTH_MODE=dev

# --- JWT public key (仅 MCP_GATEWAY_AUTH_MODE=jwt 时需要) ---
# 生产环境设置方式:
#   1. 生成密钥对:
#      openssl genrsa -out jwt-private.pem 2048
#      openssl rsa -in jwt-private.pem -pubout -out jwt-public.pem
#   2. 配置:
#      MCP_JWT_PUBLIC_KEY=file:/etc/mcp-gateway/jwt-public.pem
#      MCP_GATEWAY_AUTH_MODE=jwt
#MCP_JWT_PUBLIC_KEY=file:/etc/mcp-gateway/jwt-public.pem
#MCP_JWT_ISSUER=mcp-gateway
